<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pmdm_ut05_tarea">

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.PmdmUt05Tarea"
        android:label="@string/app_name">

        <activity android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PmdmUt05Tarea">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DetalleHeroeActivity"
            android:theme="@style/Theme.PmdmUt05Tarea.NoActionBar"/>
        <activity android:name=".AddHeroActivity" />
    </application>

</manifest>
